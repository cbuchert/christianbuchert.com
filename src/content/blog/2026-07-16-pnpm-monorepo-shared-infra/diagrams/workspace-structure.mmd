---
title: "Supplementary View — Monorepo Workspace Structure (Code Organization)"
---
flowchart TB
  subgraph monorepo["PNPM Workspace Monorepo"]
    subgraph pkgs["packages/ — shared, not deployed"]
      subgraph config["config/ — shared configuration"]
        designTokens["`**Design Tokens**
Color, spacing, and typography primitives`"]
        lintConfig["`**Lint Config** · _ESLint / Biome_
Shared linting and formatting rules`"]
        testConfig["`**Test Config** · _Vitest / Playwright_
Unit and e2e shared test configuration`"]
        tsConfig["`**TypeScript Config** · _tsconfig.json_
Shared base TS configurations`"]
      end
      subgraph framework["framework/ — framework building blocks"]
        components["`**Component Library**
Shared UI components`"]
        designSystem["`**Design System**
Themed component compositions`"]
        i18n["`**i18n** · _i18next_
Internationalization infrastructure`"]
        testFramework["`**Test Framework** · _Vitest_
Shared test utilities and setup`"]
        lintInfra["`**Lint Infrastructure** · _ESLint / Biome_
Custom lint rules and plugins`"]
      end
      subgraph libs["lib/ — shared library functions"]
        logging["`**Logging** · _TypeScript_
Structured logging utilities`"]
      end
    end
    subgraph appsBoundary["apps/ — built and deployed"]
      app["`**App(s)** · _various build targets_
Independently built and deployed projects — SPAs, SSR/static sites, services, etc.`"]
      storybook["`**Storybook**
Shared component explorer — pulls stories from across the monorepo`"]
    end
  end

  subgraph legend["Legend — Import Alias Convention"]
    aliasIntra["`**\~ intra-workspace**
Resolves within the same workspace (e.g. an app's own utils)`"]
    aliasCross["`**@ cross-workspace**
Resolves across workspaces (every arrow below)`"]
  end

  app -->|"imports"| components
  app -->|"imports"| designSystem
  app -->|"imports"| i18n
  app -->|"imports"| logging
  storybook -->|"collects stories from"| components
  storybook -->|"collects stories from"| designSystem
  components -->|"imports"| designTokens
  designSystem -->|"composes"| components
  designSystem -->|"imports"| designTokens
  lintInfra -->|"extends"| lintConfig
  testFramework -->|"extends"| testConfig
